# A simple example of ClusterConfig object:
---
apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: expense
  region: us-east-1

ManagednodeGroups:
  - name: expense
    instanceType: ["m5.large","t3.small", "t3.medium"]
    desiredCapacity: 3
    spot : true